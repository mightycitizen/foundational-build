{% if section_button | default %}
  {% set section_button = section_button | merge({
    "modifier": "secondary"
  }) %}
{% endif %}

{% include '@components/sections/section.twig' with {
  section_class: "cta bg-primary bg-gradient-primary-darker color-white " ~ class | default ~ (section_image | default ? ' cta--horizontal': ''),
  intro_class: (section_image | default ? 'medium-text-left': '') ~ ' text-center mb-0'
}%}
