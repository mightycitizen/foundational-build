<div class="form-input">
  <label for="{{ id }}">{{ label }} {% if required | default %}*{% endif %}</label>
  {% block field %}
  {% endblock %}
  {% if required | default %}
    <span class="form-error">{{ label }} is required</span>
  {% endif %}
</div>
