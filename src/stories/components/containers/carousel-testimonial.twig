

  {% embed '@components/containers/slick.twig' with {
    slick_class: 'slick-slider--equalize'
  } %}
    {% block slide_content %}
      {% for card in cards %}
          <div>
            {% set card = card | merge({
              lazyload_class: 'data-lazy'
            }) %}
            {% include '@components/teasers/card-testimonial.twig' with card %}

          </div>
        {% endfor %}
    {% endblock %}
  {% endembed %}

