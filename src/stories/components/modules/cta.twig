{% if button | default %}
  {% set button = button | merge({
    modifier: "secondary"
  }) %}
{% endif %}

{% include '@components/sections/section.twig' with {
  class: "cta bg-solid-primary text-white " ~ class | default ~ (image | default ? ' cta--horizontal': ''),
  intro_class: (image | default ? 'medium-text-left': '') ~ ' text-center m-b-0'
}%}
