

<ul id="{{ accordion_id | default }}" class="list-unstyled accordion {% if modifier | default  %}accordion--{{ modifier | default }}{% endif %}" >

  {% for item in items %}
    {% include '@components/containers/accordion/accordion-item.twig' with {item: item, modifier: modifier} %}
  {% endfor %}
</ul>
