<ul class="inline-list inline-list--divided text-smaller font-bold {{ meta_class | default }}">
  {% if date | default %}
    <li class="label">
      <time datetime="{{ date.unformatted }}">{{ date.full }}</time>
      {% if endDate | default %}
        &mdash;
        <time datetime="{{ endDate.unformatted }}">
          {{ endDate.full }}
        </time>

      {% endif %}
    </li>
  {% endif %}

  {% if categories | default | length %}
    <li>
      <ul class="inline-list inline-list--top">
        {% for category in categories %}<li><a href="{{ category.url }}">{{ category.title }}</a></li>{% endfor %}
      </ul>
    </li>
  {% endif %}
</ul>
