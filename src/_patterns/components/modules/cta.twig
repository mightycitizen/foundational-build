{% if section_button | default %}
  {% set section_button = section_button | merge({
    "modifier": "secondary"
  }) %}
{% endif %}

{% include patternlab_namespace | default ~ 'components/sections/section.twig' with {
  section_class: "cta bg-solid-primary text-white " ~ class | default ~ (section_image | default ? ' cta--horizontal': ''),
  intro_class: (section_image | default ? 'medium-text-left': '') ~ ' text-center mb-0'
}%}
