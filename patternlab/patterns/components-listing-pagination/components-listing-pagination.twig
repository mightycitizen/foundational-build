<nav aria-label="Pagination">
  <ul class="pagination">
    {% if links.previous %}
      <li class="pagination-previous"><a href="{{ links.previous }}" aria-label="Previous page"><span class="show-for-sr">Previous page</span></a></li>
    {% endif %}
    {% if links.pages | length %}
      {% for link in links.pages %}
        <li>
          {% if link.ellipses | default %}
            &hellip;
          {% elseif link.current | default %}
            <strong><span class="show-for-sr">Page</span> {{ link.pageIndex }}</strong>
          {% else %}
            <a href="{{ link.url }}" aria-label="Page {{ link.pageIndex }}"><span class="show-for-sr">Page</span> {{ link.pageIndex }}</a>
          {% endif %}
        </li>
      {% endfor %}
    {% endif %}
    {% if links.next %}
      <li class="pagination-next"><a href="{{ links.next }}" aria-label="Next page"><span class="show-for-sr">Next page</span></a></li>
    {% endif %}
  </ul>
</nav>
