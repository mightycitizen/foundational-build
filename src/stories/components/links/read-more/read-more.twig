{% set modifier = 'font-bold no-underline hover:underline' %}
{% if size == 'lg' %}
  {% set modifier = modifier ~ ' text-2xl' %}
{% endif %}
  {% include '@components/links/link.twig' with {
    url: url,
    text: text,
    target: target,
    modifier: 'read-more' ~ (modifier | default ? ' ' ~ modifier : ''),
  } only %}
