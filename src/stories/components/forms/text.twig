{% embed '@components/forms/input.twig' %}
  {% block field %}
    <input {% if disabled | default %}disabled{% endif %} type="text" value="{{ value }}" name="{{ name }}" id="{{ field_id }}" placeholder="{{ placeholder }}" {% if required | default %}required{% endif %}/>
  {% endblock %}
{% endembed %}
